# 1. Login in Nexus with User S00002094 (Password see MargunKeePass).
# 2. Create UserToken. Copy the token user:password (lowest)
# 3. Encode with echo -n "token" | base64
# 4. Insert the result in nexus-pro-docker-credentials-template.yaml (Replace xxx)
{
    "auths": {
      "toolchain-docker.nexus.bit.admin.ch": {
        "auth": "xxx"
      }
    }
}

